# Changelog: protobuf-grpc.md

## Pass 1: Structural / Developmental
- 7 comments. Themes:
  - Chunking (concept → snippet → explanation) is strong; no restructure recommended.
  - The "Why gRPC" section correctly pre-empts the REST vs gRPC question experienced readers will ask — placement is right.
  - Exercise has clean scope (proto-only) and verifiable success criteria.
  - Good use of Java/Kotlin bridge analogies for the target reader profile.
  - The `mustEmbedUnimplementedCatalogServiceServer` explanation is a highlight — keep as-is.

## Pass 2: Line Editing
- **Line ~3 (lede):** "Before writing a single line of service logic..." — consider tightening the "a single line of" cliché; optional.
- **Line ~22:** Drop "with naked eyes."
  - Before: "inspect the response with naked eyes the way you can..."
  - After: "inspect the response the way you can..."
  - Reason: Colloquial phrase adds no meaning.
- **Line ~22:** Postman/Insomnia gRPC support is no longer a new/notable distinction; consider rewording so the line does not date quickly.
- **Line ~30:** "big-bang redeploy" informal — optional swap to "coordinated redeploy".
- **Line ~36:** "The question isn't 'why not REST'..." — soften rhetorical flourish.
- **Line ~42:** Drop colloquial "fly".
  - Before: "Multiple RPC calls can fly over a single TCP connection..."
  - After: "Multiple RPC calls share a single TCP connection..."
- **Line ~48:** Parenthetical awkwardness.
  - Before: "external HTTP requests from the browser hit the API Gateway (or directly the service, depending on routing)"
  - After: "External HTTP requests reach the API Gateway (or, for some routes, a service directly)"
- **Line ~60:** "Proto3 is the current version and the one you should use for new projects." — tighten to "Proto3 is current; use it for new projects."
- **Line ~77:** Passive-to-active.
  - Before: "Without this, the generated code would be placed in a location that's harder to import correctly."
  - After: "Without this, the generator picks a default path that's awkward to import."
- **Line ~98:** Vague tail.
  - Before: "equivalent to a Java `interface` or a Kotlin `interface` annotated with some RPC framework"
  - After: "equivalent to a Java or Kotlin interface annotated for an RPC framework (e.g., gRPC's @GrpcService)"
- **Line ~100:** "instead of nothing" → "rather than a void/Empty type."
- **Line ~133:** "On the wire it encodes as a sequence of values for that field number" → "On the wire, it encodes as a sequence of values tagged with that field number."
- **Line ~139:** Negative-lead rewrite.
  - Before: "In theory you can use the raw `protoc` compiler with downloaded plugins to generate code. In practice, `protoc` dependency management is painful:"
  - After: "You can use the raw `protoc` compiler with downloaded plugins. In practice, its dependency management is painful:"
- **Line ~166:** Weak tail.
  - Before: "which is valid design"
  - After: "which is an intentional design choice"
- **Line ~232:** Split long sentence on DO NOT EDIT.
  - Before: "The `// Code generated by protoc-gen-go. DO NOT EDIT.` header at the top means exactly what it says — any edits you make will be overwritten the next time you run `buf generate`."
  - After: "The `// Code generated...DO NOT EDIT.` header is load-bearing — `buf generate` will overwrite anything you change."
- **Line ~252:** Sentence > 40 words. Restructure: break into two sentences; see changelog line above.
- **Line ~311:** Redundant "interface".
  - Before: "in both the `CatalogServiceServer` interface and the `CatalogServiceClient` interface"
  - After: "in both the server and client interfaces"
- **Line ~324 (summary bullet):** Triple-stacked modifier "dependency-managed, linting, breaking-change-detecting" is hard to parse. Rewrite to a list of verbs. See annotated file.

## Pass 3: Copy Editing
- **Line ~17:** "3–10x smaller on average" — en dash correct (CMOS 6.78). "x" multiplier acceptable.
- **Line ~43:** "not at 2am" — change to "not at 2 a.m." (CMOS 9.38).
- **Line ~60:** "proto3" lowercase when mid-sentence (language spec convention). Audit — mostly compliant.
- **Line ~122:** "most-frequently-used fields" — hyphenation correct (CMOS 7.81).
- **Line ~122:** Serial-comma compliance verified (CMOS 6.19).
- References list — verify URLs live and canonical (queries, not changes).

### Factual queries (Please verify)
- **Line ~17:** The "3–10x smaller on average" claim — source and qualification.
- **Line ~60:** Is it still accurate to say "all fields are optional by default" without mentioning the `optional` keyword reintroduced in proto3.15+?
- **Line ~122:** Byte ranges for field-number encoding — confirm 1–15 = 1 byte, 16–2047 = 2 bytes is still current; noting that 2048+ uses more bytes.
- **Line ~189:** `paths=source_relative` flag name for current buf v2 plugin config.
- **References:** All three URLs — confirm buf docs URL is still canonical.

## Pass 4: Final Polish
- No typos, doubled words, or broken cross-references found.
